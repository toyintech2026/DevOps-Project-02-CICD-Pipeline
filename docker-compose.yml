services:
  nodejs-app:
    build: .
    container_name: devops-nodejs-app-compose
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped